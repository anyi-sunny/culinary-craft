Drop profile avatar icons (PNG with transparency recommended) into this
folder, then list their filenames in src/lib/profileIcons.js:

    export const PROFILE_ICONS = ['chef-hat.png', 'whisk.png'];

The profile page's icon picker renders straight from that list.
